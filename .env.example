# ===========================================
# PCD Training App - Environment Variables
# ===========================================

# PocketBase (self-hosted)
# Get these from your self-hosted PocketBase dashboard
VITE_POCKETBASE_URL=http://your-pocketbase-url:8090

# ===========================================
# Infrastructure (Coolify service config)
# These are NOT used by the app directly.
# Set them on the respective Coolify services.
# ===========================================

# PocketBase Service (Coolify)
# No additional env vars needed for PocketBase
# Just run the single container with:
# docker run -d -p 8090:8090 ghcr.io/pocketbase/pocketbase:latest
